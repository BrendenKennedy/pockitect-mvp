{
  "project": {
    "name": "test-postgres-app",
    "description": "Web app with PostgreSQL database",
    "region": "us-east-1",
    "owner": "tester"
  },
  "network": {
    "vpc_mode": "new",
    "vpc_cidr": "10.0.0.0/16",
    "subnet_type": "public",
    "rules": [
      { "port": 22, "protocol": "tcp", "cidr": "0.0.0.0/0", "description": "SSH" },
      { "port": 80, "protocol": "tcp", "cidr": "0.0.0.0/0", "description": "HTTP" },
      { "port": 443, "protocol": "tcp", "cidr": "0.0.0.0/0", "description": "HTTPS" },
      { "port": 5432, "protocol": "tcp", "cidr": "10.0.0.0/8", "description": "PostgreSQL (private)" }
    ],
    "status": "pending"
  },
  "compute": {
    "instance_type": "t3.small",
    "image_id": "ubuntu-22.04",
    "user_data": "#!/bin/bash\napt update && apt install -y postgresql-client nginx\n",
    "status": "pending"
  },
  "data": {
    "db": {
      "engine": "postgres",
      "instance_class": "db.t3.micro",
      "allocated_storage_gb": 20,
      "username": "admin",
      "status": "pending"
    },
    "s3_bucket": { "status": "skipped" }
  },
  "security": {
    "key_pair": {
      "mode": "generate",
      "name": "test-postgres-app-key",
      "status": "pending"
    },
    "iam_role": {
      "enabled": true,
      "role_name": "test-postgres-app-role",
      "status": "pending"
    }
  }
}
